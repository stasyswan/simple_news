<tr>
  <td><%= tag.name %></td>
  <td><%= link_to 'Destroy', destroy_tag_path(:id => @news_article.id, :tag_id => tag.id), method: :delete, :remote => true, :class => "btn btn-danger", data: { confirm: 'Are you sure?' } %></td>
</tr>